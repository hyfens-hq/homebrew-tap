# Hyfens Homebrew Tap

Install the Hyfens CLI from the immutable GitHub Release archive:

```bash
brew tap hyfens-hq/tap
brew install hyfens
hyfens --version
```

The formula is generated from the published release checksums. The `tool`
command remains only as a deprecated compatibility shim; use `hyfens`.
